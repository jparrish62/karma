<% if @commentable.is_a?(Work) %>

  $("#karma_comments_first_level").append("<%= j (render partial: 'comments/comment', object: @comment) %>");

<% else %>

  $("#karma_comments_<%= @commentable.id %>").append("<%= j (render partial: 'comments/comment', object: @comment) %>");

<% end %>
